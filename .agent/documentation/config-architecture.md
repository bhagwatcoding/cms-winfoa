# Config Architecture Diagram

## 📊 New Config Structure

```
┌─────────────────────────────────────────────────────────────┐
│                     Application Start                        │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              src/config/env.ts (Zod Validation)              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ✅ Validates environment variables at startup           │ │
│  │ ✅ Fails fast with clear errors                         │ │
│  │ ✅ Type-safe environment access                         │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
│  Exports: env { NODE_ENV, MONGODB_URI, SESSION_SECRET, ... } │
└──────────────────────┬──────────────────────────────────────┘
                       │
         ┌─────────────┼─────────────┬──────────────┬─────────┐
         │             │             │              │         │
         ▼             ▼             ▼              ▼         ▼
┌─────────────┐ ┌───────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│  site.ts    │ │  auth.ts  │ │  db.ts   │ │limits.ts │ │ index.ts │
├─────────────┤ ├───────────┤ ├──────────┤ ├──────────┤ ├──────────┤
│             │ │           │ │          │ │          │ │          │
│ SITE        │ │ SESSION   │ │ DB       │ │ UPLOAD   │ │ Re-export│
│ ├─ NAME     │ │ ├─ SECRET │ │ ├─ URI   │ │ PAGINATION│ │ all      │
│ ├─ DESC     │ │ ├─ COOKIE │ │ ├─ NAME  │ │ RATE_LIMIT│ │ config   │
│ ├─ VERSION  │ │ │  _NAME  │ │ ├─ OPTIONS│ │          │ │ modules  │
│ └─ URL      │ │ ├─DURATION│ │ └─ COLLS │ │          │ │          │
│             │ │ └─ COOKIE │ │          │ │          │ │          │
│ TENANCY     │ │    _OPTIONS│ │          │ │          │ │          │
│ ├─ ENABLED  │ │           │ │          │ │          │ │          │
│ ├─ ROOT_DOM │ │ SECURITY  │ │          │ │          │ │          │
│ ├─ COOKIE   │ │ ├─ BCRYPT │ │          │ │          │ │          │
│ │  _DOMAIN  │ │ ├─ MAX_   │ │          │ │          │ │          │
│ └─ RESERVED │ │ │  LOGIN  │ │          │ │          │ │          │
│    _SUBS    │ │ └─ LOCKOUT│ │          │ │          │ │          │
└─────────────┘ └───────────┘ └──────────┘ └──────────┘ └──────────┘
```

## 🔄 Usage Flow

```
┌────────────────────────────────────────────────────────────────┐
│                      Your Application Code                      │
└────────────────────────────────────────────────────────────────┘
                                │
                                │ import { SESSION, DB, env } from '@/config'
                                ▼
┌────────────────────────────────────────────────────────────────┐
│                     src/config/index.ts                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  export * from './env'                                    │  │
│  │  export * from './site'                                   │  │
│  │  export * from './auth'                                   │  │
│  │  export * from './db'                                     │  │
│  │  export * from './limits'                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
    ┌─────────────────┐  ┌──────────┐   ┌──────────────┐
    │  Type Safety    │  │ Validated│   │ Single Source│
    │  via 'as const' │  │   Env    │   │  of Truth    │
    └─────────────────┘  └──────────┘   └──────────────┘
```

## 📦 Old vs New Comparison

### ❌ Old Approach (Scattered)
```
Application Code
     │
     ├─ process.env.MONGODB_URI (direct access)
     ├─ process.env.SESSION_SECRET (direct access)
     ├─ Hardcoded: "auth_session" (cookie name)
     ├─ Hardcoded: 7 * 24 * 60 * 60 * 1000 (session duration)
     └─ Inconsistent cookie options across files

Problems:
❌ No validation
❌ Runtime errors
❌ Type unsafe
❌ Duplicated logic
❌ Hard to test
❌ Environment-specific code scattered
```

### ✅ New Approach (Centralized)
```
Application Code
     │
     └─ import { SESSION, DB, env } from '@/config'
              │
              ├─ SESSION.COOKIE_NAME: "w_sid"
              ├─ SESSION.DURATION: 2592000 (30 days in seconds)
              ├─ SESSION.COOKIE_OPTIONS: { httpOnly, secure, ... }
              ├─ DB.URI: validated URL
              ├─ DB.NAME: "winfoa_core"
              └─ env.NODE_ENV: 'development' | 'production' | 'test'

Benefits:
✅ Validated at startup (Zod)
✅ Type-safe ('as const')
✅ Single source of truth
✅ Easy to mock for testing
✅ Consistent across codebase
✅ Environment logic centralized
```

## 🔐 Security Benefits

### Cookie Domain Strategy
```
┌─────────────────────────────────────────────────────────┐
│                    TENANCY.COOKIE_DOMAIN                 │
└─────────────────────┬───────────────────────────────────┘
                      │
        ┌─────────────┴──────────────┐
        │                            │
        ▼                            ▼
┌───────────────┐          ┌──────────────────┐
│ Development   │          │   Production     │
├───────────────┤          ├──────────────────┤
│ undefined     │          │ .winfoa.com      │
│               │          │                  │
│ ✅ localhost   │          │ ✅ Share cookies  │
│   compatible  │          │   across        │
│               │          │   subdomains    │
│ ✅ Easy testing│          │                  │
└───────────────┘          │ ✅ auth.winfoa.com│
                           │ ✅ api.winfoa.com │
                           │ ✅ app.winfoa.com │
                           └──────────────────┘
```

### Session Cookie Evolution
```
Old: SESSION_COOKIE_NAME = "auth_session"
     ❌ Obvious name
     ❌ Security through obscurity ignored

New: SESSION.COOKIE_NAME = "w_sid"
     ✅ Obscured name
     ✅ Harder for attackers to identify
     ✅ Similar to industry standards (connect.sid, etc.)
```

## 🏗️ Application Layers

```
┌────────────────────────────────────────────────────────────┐
│                      User Request                           │
└──────────────────────┬─────────────────────────────────────┘
                       │
┌──────────────────────▼─────────────────────────────────────┐
│                    Next.js App                              │
│  ┌────────────────────────────────────────────────────────┐│
│  │  Pages / Components / Server Actions                   ││
│  │     │                                                   ││
│  │     └─ import { SESSION, DB, env } from '@/config'     ││
│  └────────────────────────────────────────────────────────┘│
└──────────────────────┬─────────────────────────────────────┘
                       │
┌──────────────────────▼─────────────────────────────────────┐
│                  Config Layer (@/config)                    │
│  ┌────────────────────────────────────────────────────────┐│
│  │ env.ts → Validates environment at startup              ││
│  │ site.ts → Site config + Multi-tenancy                  ││
│  │ auth.ts → Session + Security                           ││
│  │ db.ts → Database config                                ││
│  │ limits.ts → Rate limits, uploads, pagination           ││
│  └────────────────────────────────────────────────────────┘│
└──────────────────────┬─────────────────────────────────────┘
                       │
┌──────────────────────▼─────────────────────────────────────┐
│              Environment Variables (.env.local)             │
│  ┌────────────────────────────────────────────────────────┐│
│  │ NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000                 ││
│  │ MONGODB_URI=mongodb://localhost:27017/winfoa           ││
│  │ SESSION_SECRET=<long-random-string>                    ││
│  └────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────┘
```

## 🎯 Import Patterns

### Recommended
```typescript
// Import everything you need
import { SESSION, DB, TENANCY, env } from '@/config';

// Use with type safety
const cookieName = SESSION.COOKIE_NAME; // Type: "w_sid"
const dbUri = DB.URI; // Type: string
const isProd = env. NODE_ENV === 'production'; // Type: boolean
```

### Also Valid
```typescript
// Import specific modules
import { SESSION } from '@/config/auth';
import { DB } from '@/config/db';
import { env } from '@/config/env';
```

### ❌ Avoid
```typescript
// Don't access process.env directly anymore
const uri = process.env.MONGODB_URI; // ❌ No validation
const secret = process.env.SESSION_SECRET; // ❌ Could be undefined
```

## 📈 Migration Progress

```
Total Files Using Old Constants: ~40

┌────────────────┬──────┬─────────┐
│ Category       │ Done │ Pending │
├────────────────┼──────┼─────────┤
│ Core           │  3   │    0    │ ✅ Complete
│ Services       │  0   │   12    │ 🔄 In Progress
│ Features/Auth  │  0   │   15    │ 📋 Planned
│ API Routes     │  0   │    8    │ 📋 Planned
│ Other          │  0   │    5    │ 📋 Planned
└────────────────┴──────┴─────────┘

Progress: ██░░░░░░░░ 7.5% (3/40)
```
